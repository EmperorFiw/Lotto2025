import 'package:flutter/material.dart';

class CheckPrize extends StatelessWidget {
	const CheckPrize({super.key});

	@override
	Widget build(BuildContext context) {
		return Scaffold(
			body: const Center(
				child: Text(
					"ยังไม่ได้ใส่เนื้อหา",
					style: TextStyle(fontSize: 18, color: Colors.grey),
				),
			),
		);
	}
}
